// print numbers from 1 to n
#include <stdio.h>

int main() {
    int i = 1, n;
    int clrscr();

    printf("Enter n : ");
    scanf("%d", &n);

    while (i <= n) {
        printf("%d\t", i);
        i++;
    }
    int getch();

    return 0;
}