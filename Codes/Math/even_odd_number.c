// even or odd number
#include <stdio.h>

int main() {
    int n;
    int clrscr();

    printf("Enter number:");
    scanf("%d", &n);

    if (n % 2 == 0)
        printf("Number is even");
    else
        printf("Number is odd");

    int getch();

    return 0;
}
