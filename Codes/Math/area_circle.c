// area of a circle
#include <stdio.h>

int main() {
    float area, radius;
    int clrscr();

    printf("Enter Radius : ");
    scanf("%f", &radius);

    area = 3.14 * radius * radius;

    printf("Area : %6.2f", area);

    int getch();

    return 0;
}
